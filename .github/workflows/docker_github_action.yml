name: Github_action

on:  
  push:
    branches: [main]
  pull_request:  
    branches: [main]   
    
jobs:
  build:
    runs-on: alpine:3.14
    steps:
      - uses: docker/build-push-action@v5
      - name: Docker image
        run: docker buid . --file Dockerfile --tag my-image name:$(date+%s)
